<Page
    x:Class="ScannerApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ScannerApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    Loaded="MainPage_Loaded">

    <Grid Margin="20">
        <!--
        <ComboBox x:Name="choiceCombo" Header="選択肢"/>
        <Button x:Name="runButton" Content="実行" Click="RunButton_Click"/>
        <TextBlock x:Name="OutputTextBlock" Text="ここに結果が表示されます"/>

        <Rectangle Height="1" Fill="Gray" Margin="0,20"/>
        -->
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0" Spacing="10" Margin="0,0,20,0">

            <TextBlock Text="スキャナー" FontWeight="Bold"/>
            <Button x:Name="ScanDeviceButton" 
                    Content="スキャナーを検索" 
                    Click="ScanDeviceButton_Click"/>
            <ComboBox x:Name="scannerCombo" Header="利用可能なスキャナー"/>
            <Button x:Name="ExecuteScanButton" 
                        Content="スキャン実行" 
                        Click="ExecuteScanButton_Click" 
                        HorizontalAlignment="Left"/>
            <TextBlock x:Name="ScannerStatusTextBlock" Text="「スキャナーを検索」ボタンを押してください"/>
            <Border BorderBrush="Gray" BorderThickness="1" Margin="0,10" Height="400">
                <Image x:Name="ScannedImage" Stretch="Uniform"/>
            </Border>
            <Button x:Name="ExecutePrintButton" 
                    Content="プリント実行" 
                    Click="ExecutePrintButton_Click"/>
            <TextBlock x:Name="PrintStatusTextBlock" Text=""/>
        </StackPanel>

        <StackPanel Grid.Column="1" Spacing="10">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="UrlTextBox" Header="表示したいWebページのURLを入力" Text="https://www.google.com" VerticalAlignment="Bottom"/>
                <Button x:Name="GoButton" Content="移動" Grid.Column="1" Click="GoButton_Click" VerticalAlignment="Bottom"/>
            </Grid>
            <controls:WebView2 x:Name="MyWebView" Height="600"/>
        </StackPanel>
        
    </Grid>

</Page>
